# Secret integration credentials only. Copy to .env on the deployment host.
# deploy.sh never overwrites an existing .env.

# Create a dedicated least-privilege API token in Zabbix and set it here.
ZABBIX_API_TOKEN=
